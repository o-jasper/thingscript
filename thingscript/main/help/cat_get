# Command `cat_get`
`get` but immediately outputs the file to stdout;

Effectively:

    cat "`thingscript get $input`"

The quotes `"` matter; otherwise finding nothing will make it output the input
from stdin at that time.(potentially very confusing)
