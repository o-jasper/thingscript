# Sub element directories.

`el/sel` contains elements that elements use; sub elements.
$a el/sel/gallery$ for instance has different subelements. 

Each element that has subelements should use a directory here.
