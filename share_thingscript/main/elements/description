#!/bin/bash

FILE=`thingscript get description`

if [ "$FILE" != "" ]; then
   cat $FILE
else 
   echo $FILE > /dev/stderr 
   if [ -e readme.md ]; then  #TODO more flexibility
      markdown readme.md
   fi
fi
