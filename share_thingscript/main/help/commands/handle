# Command `handle`
Goes through a file looking for `$` characters, when it takes the text from the
`$` sign to another `$` sign or a newline to be a command to run a program
in `el/`, with space-separated arguments.

Commands ending in `_` or `.cat` are taken a little differently, see `el`.
