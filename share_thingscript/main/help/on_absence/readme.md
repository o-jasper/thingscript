If `thingscript get ...` cannot find a file, it will use the programs in this
directory to generate a default and put it in the local `thingscript/`.

$related concepts/thingscript_directory vars/THINGSCRIPT_PATH$
